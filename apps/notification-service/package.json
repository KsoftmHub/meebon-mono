{
  "name": "notification-service",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "echo 'Starting notification-service (dev mode)' && tsc --watch & node dist/index.js",
    "lint": "echo 'Running lint for notification-service' && exit 0",
    "test": "echo 'Running test for notification-service' && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "shared": "workspace:*",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
