{
  "name": "express-app",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "echo 'Starting express-app (dev mode)' && tsc --watch & node dist/index.js",
    "lint": "echo 'Running lint for express-app' && exit 0",
    "test": "echo 'Running test for express-app' && exit 0"
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "latest"
  }
}
