{
  "name": "cli",
  "version": "1.0.0",
  "private": true,
  "description": "CLI tool for the ecommerce monorepo",
  "bin": {
    "monorepo-cli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "lint": "echo 'Running lint for cli' && exit 0",
    "test": "echo 'Running test for cli' && exit 0"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "fs-extra": "^11.2.0",
    "chalk": "^4.1.2",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "latest",
    "@types/prompts": "^2.4.9",
    "typescript": "latest"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts"
}
